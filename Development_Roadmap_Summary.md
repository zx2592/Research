# AlphaSystem 演进路线图 (下一步计划)

根据 `20260305_SystemInsights.md` 文件的记录（截至 2026-03-06 的情况），Phase 6 (闭环接线阶段) 已经全部完成。接下来的开发计划主要集中在 Phase 7 到 Phase 9，其核心逻辑是从「半自动」演进到「用户只下判断」的投资副驾驶形态。

以下是针对下一步（Phase 7 及以后）的说明总结：

## 🟢 Phase 7: 主动智能 (Active Intelligence)
**核心目标**: 从「你问它答」变成「它发现了，来告诉你」。让你距离"只下判断"更近。

1. **异动扫雷快评 (已部分完成)**
   - 将守护脚本 (Sentinel) 直接嵌入到 `/scan` 工作流中。在生成每日扫描报告时，自动甄别涨跌幅 >4% 的核心资产，输出《急诊快评》专项段落。
2. **财报日历提醒**
   - 每周日自动扫描持仓，找出下一周有财报的标的，提前生成 `/radar` 监控清单并推送。
3. **晨报自动推送**
   - 每个交易日早晨 8:00，自动执行 `/scan` 扫描，并将精简版直接推送到 Telegram。
4. **持仓日报/周报**
   - 每周末自动执行 `/position` 体检，汇总组合健康度、盈亏和风控预警，并主动推送。

## 🟡 Phase 8: 深度增强 (Deep Enhancements)
**核心目标**: 让 Agent 的分析质量逼近买方真人员工。

1. **多标的横向筛选 `/screen`**
   - 根据条件（如 PE < 20, 营收增速 > 15%）自动从股票池中筛选、排名，生成候选池。
2. **产业链映射**
   - 在 `/deep` 报告中自动识别产业链上下游（供应商/竞品），并将其与你的现有持仓或监控池建立映射关系。
3. **知识库自动消化**
   - 生成深度报告或公司更新时，Agent 自动提取结论并更新知识库卡片（目前还需要手动用 `/add` 命令）。
4. **历史报告回溯增强**
   - 执行买卖审计 (`/buy`, `/sell`) 时，自动回溯其所有的历史研报，生成「逻辑演变时间线」，严防逻辑漂移（Style Drift）。

## 🔴 Phase 9: 终极形态 (Investment Copilot)
**核心目标**: 成为真正的投资副驾驶。

1. **组合级别的「如果分析」 (What-If)**
   - 比如：“如果某只股票大跌20%同时加息，我的组合会怎样？”进行情景压力测试。
2. **交易信号聚合仪表盘**
   - 将所有持仓的技术面、基本面和情绪面信号，汇聚成一个每日更新的统一视图面板。
3. **智能再平衡建议**
   - 基于设定的仓位上限和资产相关性矩阵，Agent 自动计算需要调仓的具体股数，用户一键确认即可执行。
4. **量化系统对接**
   - 将 AlphaSystem 的文本研究成果、估值锚点通过标准化接口倒出给量化回测系统，实现「基本面研究 + 量化交易」的无缝咬合。

---
**资料来源**: `20260305_SystemInsights.md`
